# cargo-audit configuration

[advisories]
ignore = [
    "RUSTSEC-2023-0071", # rsa (Marvin Attack): No fix in 0.9 range used by sqlx-mysql (transitive). We use PostgreSQL.
    "RUSTSEC-2025-0134", # rustls-pemfile is unmaintained. Upstream (async-nats, bollard) needs to update.
]
